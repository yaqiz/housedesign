# House Design

Static tablet-friendly 3D house design app.

## Deploy on Netlify

Use Netlify's GitHub import flow:

- Repository: `yaqiz/housedesign`
- Base directory: leave empty
- Build command: leave empty
- Publish directory: `.`

The entry page is `index.html`.
